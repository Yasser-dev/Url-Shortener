<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Url Shortener</title>
    <link rel="icon" href="favicon.ico" type="image/x-icon" />
    <!-- Compiled and minified CSS -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
    />
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <div class="row center-align">
      <div class="col s12">
        <div class="card blue-grey darken-1">
          <div class="card-content white-text">
            <span class="card-title">URL Shortener</span>
            <p>Enter your URL and get a shortened version of it ^^</p>
          </div>
        </div>
      </div>
    </div>
    <div class="container center-align">
      <form action="/" method="POST" autocomplete="off">
        <div class="input-field blue-grey-text">
          <i id="url-icon" class="material-icons prefix">insert_link</i>
          <label for="url">URL:</label>
          <input
            id="url-input"
            required
            type="url"
            name="url"
            placeholder="Enter your Url"
            class="text-blue-grey"
          />
        </div>
        <button
          class="btn waves-effect waves-light blue-grey"
          type="submit"
          name="action"
        >
          Shorten
          <i class="material-icons right">send</i>
        </button>
      </form>

      <% if (locals.short_url) { %>

      <div class="card blue-grey lighten-1 left-align s12">
        <div class="card-content white-text">
          <span class="card-title center">
            <a href="<%=short_url%>" target="_blank" id="shortenned-url"
              ><%= short_url %></a
            >

            <a
              id="copy-btn"
              class="waves-effect waves-light btn-small btn-floating white"
              ><i class="material-icons blue-grey-text">content_copy</i>Copy</a
            >

            <span id="copied-badge" class="badge white-text"
              >copied!</span
            ></span
          >
        </div>
      </div>
      <% } %>
    </div>

    <!-- Compiled and minified JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <script src="script.js"></script>
  </body>
</html>
